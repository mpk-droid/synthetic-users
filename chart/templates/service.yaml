apiVersion: v1
kind: Service
metadata:
  name: {{ include "synthetic-users.fullname" . }}
  labels:
    {{- include "synthetic-users.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
  selector:
    {{- include "synthetic-users.selectorLabels" . | nindent 4 }}
